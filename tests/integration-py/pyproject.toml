[project]
name = "integration-py"
version = "0.1.0"
description = "Integration tests for Cobalt"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pyautogui>=0.9.54",
    "pytest>=8.4.1",
    "selenium>=4.34.2",
]
